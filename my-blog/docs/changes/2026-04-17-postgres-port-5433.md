# 2026-04-17 - Local PostgreSQL host port changed to 5433

## Summary
Updated the local PostgreSQL container to bind to host port 5433 instead of 5432.

## Why
- The host machine already had something bound to 5432.
- Using 5433 avoids conflicts and keeps local development self-contained.

## Files changed
- `docker-compose.yml`
- `.env.local`
- `.env.example`
- `README.md`

## Validation
- Local database URL and Docker port mapping now both target port 5433.
- The migration note was updated to document the new local port.
